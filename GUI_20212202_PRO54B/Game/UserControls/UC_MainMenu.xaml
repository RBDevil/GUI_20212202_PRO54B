<UserControl x:Class="Game.UserControls.UC_MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Game"
             xmlns:renderer="clr-namespace:Game.Renderer"
             mc:Ignorable="d" 
             Height="600" Width="600"
             Loaded="Window_Loaded" KeyDown="Window_KeyDown"
             x:Name="window">
    <Grid Background="Black">
        <renderer:Display x:Name="display" />

        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>-->
        
        <Button Content="Play" Grid.Row="0" Grid.Column="1" Click="btn_Play_Click" />
        <!--<Button Content="Exit" Grid.Row="2" Grid.Column="1" Click="btn_Exit_Click" />-->
    </Grid>
</UserControl>
